// someMath.ts
export const add = (a: number, b: number) => a + b;
const multiply = (a: number, b: number) => a * b;
export const calculate = (a: number, b: number) => {
  const addddd = add(a, b);
  const multiplyyy = multiply(a, b);
  return addddd + multiplyyy;
};
